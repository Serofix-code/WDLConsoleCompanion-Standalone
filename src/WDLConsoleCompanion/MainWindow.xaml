<Window x:Class="WDLConsoleCompanion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WDL Console Companion" Width="960" Height="620" MinWidth="700" MinHeight="420"
        Closing="Window_Closing">
  <Grid Margin="18">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Grid Margin="0,0,0,14">
      <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="#657083" Margin="0,0,9,0"/>
        <TextBlock x:Name="StatusText" Text="DETACHED" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
      </StackPanel>
      <TextBlock Grid.Column="1" Text="AUTO-INJECT ON" Foreground="#7F8DA3" FontSize="11" Margin="16,0,0,0" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="2" Text="WDL CONSOLE COMPANION" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" Margin="0,0,12,0" VerticalAlignment="Center"/>
      <Button Grid.Column="3" Content="Cheats" Click="CheatsButton_Click" Margin="0,0,8,0"/>
      <Button x:Name="ManualInjectButton" Grid.Column="4" Content="Manual Inject" Click="ManualInjectButton_Click" Margin="0"/>
    </Grid>
    <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="14">
      <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
        <TextBlock x:Name="LogText" FontFamily="Cascadia Mono,Consolas" FontSize="14" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/>
      </ScrollViewer>
    </Border>
    <Grid Grid.Row="2" Margin="0,12,0,0">
      <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
      <TextBlock Text=">" Foreground="{StaticResource AccentBrush}" FontFamily="Consolas" FontSize="20" Margin="4,2,10,0"/>
      <TextBox x:Name="CommandInput" Grid.Column="1" FontFamily="Cascadia Mono,Consolas" FontSize="15" Padding="10,7" KeyDown="CommandInput_KeyDown"/>
    </Grid>
  </Grid>
</Window>
