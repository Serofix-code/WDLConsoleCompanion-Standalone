# Examples

Examples must run without proprietary game files unless clearly marked as live-process experiments. Start with `query_database.py` to find relevant records:

```powershell
python tools/query_database.py "operative roster"
python tools/query_database.py camera
```

Live-process examples must validate build identity, pointers, and expected bytes and must be offline-only.
